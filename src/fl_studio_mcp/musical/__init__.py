"""Musical reasoning: keys, meters, time, chords and groove.

This lives on the server rather than in FL's sandbox because it needs a real
standard library, and because it is pure arithmetic that can be tested with no FL
Studio and no MIDI. The sandboxes get the facts; this layer decides what they mean.
"""
